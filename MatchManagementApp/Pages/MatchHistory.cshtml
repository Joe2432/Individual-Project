@page
@model MatchManagementApp.UI.Pages.MatchHistoryModel
@{
    ViewData["Title"] = "Match History";
}

<h2>Match History</h2>

@if (Model.MatchSummaries.Any())
{
    <table class="table table-striped">
        <thead>
            <tr>
                <th>Opponent</th>
                <th>Surface</th>
                <th>Score</th>
                <th>Status</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var summary in Model.MatchSummaries)
            {
                <tr>
                    <td>@summary.Match.FirstOpponentName</td>
                    <td>@summary.Match.Surface</td>
                    <td>@summary.ScoreSummary</td>
                    <td>@(summary.MatchOver ? "Completed" : "In Progress")</td>
                    <td>
                        @if (!summary.MatchOver)
                        {
                            <a class="btn btn-primary btn-sm" asp-page="/PlayMatch" asp-route-id="@summary.Match.Id">Continue</a>
                        }
                        else
                        {
                            <span class="text-muted">-</span>
                        }
                    </td>
                </tr>
            }

        </tbody>
    </table>
}
else
{
    <p>No matches found.</p>
}
